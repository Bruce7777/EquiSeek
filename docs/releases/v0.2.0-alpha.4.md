# EquiSeek v0.2.0-alpha.4 — 未签名 Alpha

这是面向早期试用者的未签名桌面预发布，不经过 Mac App Store 或 Microsoft Store，也没有 Apple Developer ID、Apple 公证或 Windows Authenticode 签名。

本版保留 Alpha.3 的正式投研助手布局，并修正 Windows 将 Git 文本检出为 CRLF 时无法解析 `SKILL.md` 的问题。Skill 解析现在同时支持 LF、CRLF 和 CR，且 Windows 专用回归测试通过后才生成安装包。

## 下载

- Apple Silicon Mac（M1/M2/M3/M4 等）：`EquiSeek-macOS-arm64-0.2.0-alpha.4-unsigned.zip`
- Intel Mac：`EquiSeek-macOS-x64-0.2.0-alpha.4-unsigned.zip`
- Windows 10/11 x64：`EquiSeek-Windows-x64-0.2.0-alpha.4-unsigned-Setup.exe`
- 完整性校验：`SHA256SUMS`

## 安装提示

macOS：解压后将 EquiSeek 拖入“应用程序”。首次启动请按住 Control 点击应用并选择“打开”；如果仍被拦截，请前往“系统设置 → 隐私与安全性”确认“仍要打开”。系统仍可能因企业策略禁止未签名软件。

Windows：运行 Setup.exe 后可能看到 Microsoft Defender SmartScreen 警告。确认文件来自本仓库并核对 SHA-256 后，可选择“更多信息 → 仍要运行”。企业管理设备可能禁止绕过。

## 安全与使用边界

- 不要从第三方转载站下载；只使用本 Release 的资产。
- 安装前请用 `SHA256SUMS` 核对文件完整性。
- 安装包不包含维护者或用户的 API Key、凭据文件、持仓或决策账本；凭据只在用户运行应用后由系统安全存储加密保存。
- 当前行情研究范围为中国内地 A 股及已支持的境内指数、基金；全球市场尚未接入。
- 本项目是研究与教育工具，不构成个性化投资建议、收益承诺或代客交易服务。
- 详细系统拦截提示与安装步骤见[未签名桌面版安装指南](https://github.com/Bruce7777/EquiSeek/blob/main/docs/installing-unsigned-desktop.md)。

---

This unsigned desktop alpha supersedes Alpha.3. It preserves the finalized research-assistant layout and fixes Windows CRLF parsing for built-in and user `SKILL.md` packages. The installers contain no maintainer or user API keys. macOS Gatekeeper and Windows SmartScreen may still warn or block installation.
